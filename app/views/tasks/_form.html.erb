<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :content %>
  <%= f.text_field :content %>

  <% if params[:action] == "edit" %>
    <%= f.label :completed %>
    <%= f.check_box :completed %>
  <% end %>

  <%= f.submit %>
<% end %>

